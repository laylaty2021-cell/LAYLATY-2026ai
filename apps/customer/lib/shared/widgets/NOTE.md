# shared/widgets

Cross-feature widgets (buttons, cards, empty states) go here once more than
one feature needs them — see docs/mobile/flutter-architecture.md. Nothing
here yet; auth/events screens are still small enough to inline their UI.
