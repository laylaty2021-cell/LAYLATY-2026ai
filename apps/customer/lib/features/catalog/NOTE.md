# features/catalog

Not yet implemented. Follow the same three-layer pattern as `features/auth`
and `features/events` (data/domain/presentation) once this feature is
scheduled — see `docs/mobile/flutter-architecture.md` and the matching
sprint in `docs/backlog/sprint-backlog.md`.
