import { Module } from '@nestjs/common';

// Owns: stores, store_modules (docs/database/schema.sql §2).
// Implemented in docs/backlog/sprint-backlog.md Sprint 2 (onboarding,
// business template modules). Boundary placeholder — see organizations
// module for the pattern this repo follows once implemented.
@Module({})
export class StoresModule {}
