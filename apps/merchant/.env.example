# Copy to .env.local for local development.

# Base URL of apps/api's REST contract (see docs/api/openapi.yaml). Defaults
# to http://localhost:3000/v1 when unset — see src/lib/config.ts.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/v1
